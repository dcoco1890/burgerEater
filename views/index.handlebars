
<div class="jumbotron">
    <h1 class="text-center">Burger-Eater</h1>
</div>

    <div class="row">
    <div class="col-sm-6">
        <div class="area" id="not-devoured">
            Ready to Eat
                <ul>
                    {{#each Burger}}
                    {{#unless devoured}}
                        <li>{{this.name}}          <button class="devour" data-id="{{this.id}}">Devour</button></li>
                    {{/unless}}
                    {{/each}}
                </ul>
        </div>
    </div>
    <div class="col-sm-6">
        <div class="area" id="devoured">
            No Longer Eat-able
                <ul>
                    {{#each Burger}}
                    {{#if devoured}}
                      <li class="fin">{{this.name}}</li>  
                    {{/if}}
                    {{/each}}
                </ul>
            </div>
        </div>
          
    </div>
  

